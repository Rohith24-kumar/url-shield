version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    container_name: url-shield-backend

  frontend:
    build: .
    ports:
      - "80:80"
    depends_on:
      - backend
    container_name: url-shield-frontend
